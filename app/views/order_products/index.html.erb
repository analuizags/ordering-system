<p id="notice"><%= notice %></p>

<h1>Listing Order Products</h1>

<table>
  <thead>
    <tr>
      <th>ID</th>
      <th>Quantity</th>
      <th>Note</th>
      <th>Work Shift</th>
      <th>Order</th>
      <th>Product</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @order_products.each do |order_product| %>
      <tr>
        <td><%= order_product.id %></td>
        <td><%= order_product.quantity %></td>
        <td><%= order_product.note %></td>
        <td><%= order_product.order.work_shift.name %></td>
        <td><%= order_product.order.id %></td>
        <td><%= order_product.product.id %>-<%= order_product.product.name %></td>
        <td><%= link_to 'Show', order_product %></td>
        <td><%= link_to 'Edit', edit_order_product_path(order_product) %></td>
        <td><%= link_to 'Destroy', order_product, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Order product', new_order_product_path %>
